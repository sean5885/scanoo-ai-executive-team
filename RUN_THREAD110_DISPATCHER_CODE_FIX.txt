Fix dispatcher raw JSON replies:
- in src/agent-dispatcher.mjs
- locate executeRegisteredAgent fallback path returning FALLBACK_DISABLED
- locate dispatchRegisteredAgentCommand no-match path returning ROUTING_NO_MATCH
- change both user-facing replies to natural language text
- keep internal error/details only for internal logging or internal return fields if needed, but do not expose raw JSON in reply text
- do NOT change planner contract
- do NOT change workflow/output schema globally
- after fix, run:
  node --test tests/agent-dispatcher.test.mjs
- report:
  changed files
  whether both failing cases now pass
