Apply dispatcher fix in src/agent-dispatcher.mjs:
- replace executeRegisteredAgent fallback reply text so it returns natural language, not JSON.stringify(failureEnvelope)
- replace dispatchRegisteredAgentCommand no-match reply text so it returns natural language, not JSON.stringify(raw error object)
- keep internal error/details only for internal logic if needed, but do not expose them in reply text
- do not change planner contract
- run:
  node --test tests/agent-dispatcher.test.mjs
- then report:
  changed files
  whether agent-dispatcher.test.mjs is fully green
