Thread110 dispatcher step:
- inspect executeRegisteredAgent fallback path
- inspect dispatchRegisteredAgentCommand no-match path
- verify whether these paths bypass user-response normalization
- if bypassed, fix code so user-facing reply becomes natural language
- do NOT change planner contract
- do NOT change workflow/output schema globally
- add/update focused tests only after code path is corrected
- report:
  root cause
  changed files
  whether both dispatcher failures are fixed
